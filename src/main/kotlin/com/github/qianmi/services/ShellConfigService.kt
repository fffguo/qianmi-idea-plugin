package com.github.qianmi.services

import com.intellij.openapi.project.Project


class ShellConfigService(project: Project) {
    init {
//        val myProject = ProjectUtil.getProject(project)!!
//        val credentialAttributes = createCredentialAttributes(myProject.shellIp, myProject.shellUserName)
//
//        val credentials = Credentials(myProject.shellUserName, myProject.shellPassword)
//        PasswordSafe.instance.set(credentialAttributes!!, credentials)
    }

}
